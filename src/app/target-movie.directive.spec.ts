import { TargetMovieDirective } from './target-movie.directive';

describe('TargetMovieDirective', () => {
  it('should create an instance', () => {
    const directive = new TargetMovieDirective();
    expect(directive).toBeTruthy();
  });
});
